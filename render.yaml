# Render Blueprint – RuterLive
# https://render.com/docs/blueprint-spec
services:
  - type: web
    name: ruterlive
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: npm run start
    healthCheckPath: /health
    envVars:
      - key: NODE_VERSION
        value: 20
